# API reference

The example domain: an in-memory task list, deliberately free of any database
or external adapter, so the tests can demonstrate unit and BDD coverage without
infrastructure getting in the way.

::: task_list.tasks
